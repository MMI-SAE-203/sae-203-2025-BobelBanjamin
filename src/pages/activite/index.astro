---
import { allActivites } from "../../../backend.mjs";
import Layout from '../../layouts/Layout.astro';
import ActivityCard from "../../components/activite.astro"; // ✅ Import du composant

const activites = await allActivites();
---

<Layout>
<section class="py-12 bg-[#190A35] text-white">
    <h1 class="text-4xl font-bold text-center mb-10">📅 Toutes les Activités</h1>

    <!-- 🔹 Grille des Activités -->
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 px-6 max-w-6xl mx-auto">
        {activites.length > 0 ? (
            activites.map((activite) => (
                <ActivityCard {...activite} />
            ))
        ) : (
            <p class="text-center text-gray-400">Aucune activité disponible.</p>
        )}
    </div>
</section>
</Layout>

